<h1>Listing platforms</h1>

<table>
  <tr>
	<td><b>Name</b></td>
	<td></td>
	<td></td>
	<td></td>
  </tr>

<% @platforms.each do |platform| %>
  <tr>
    <td><%= link_to platform.name, platform %></td>
    <td><%= link_to 'Join', join_platform_path(platform) %></td>
    <td><%= link_to 'Edit', edit_platform_path(platform) %></td>
    <td><%= link_to 'Delete', platform, method: :delete, data: { confirm: 'Are you sure?' } %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Platform', new_platform_path %> |
<%= link_to 'Back', tokenlists_path %>
