<h1>Listing genres</h1>

<table>
  <tr>
	<td><b>Name</b></td>
	<td></td>
	<td></td>
	<td></td>
  </tr>

<% @genres.each do |genre| %>
  <tr>
    <td><%= link_to genre.name, genre %></td>
    <td><%= link_to 'Join', join_genre_path(genre) %></td>
    <td><%= link_to 'Edit', edit_genre_path(genre) %></td>
    <td><%= link_to 'Delete', genre, method: :delete, data: { confirm: 'Are you sure?' } %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Genre', new_genre_path %> |
<%= link_to 'Back', tokenlists_path %>
